# Orchestration Log: Rusty (Scripts)

**Date:** 2026-06-06T13:25:00-04:00
**Issue:** #123 agent-intake preflight
**Model:** claude-opus-4.8
**Mode:** sync

## Routing

**Role:** Scripts verification — hardened dry-run behavior + least-privilege path ordering on agent-intake setup scripts
**Scope:** `setup_entra_agent_id.py`, `setup_purview_retention_label.py`, `setup_purview_retention_label.ps1`

## Work

1. Re-verified both setup scripts vs Microsoft Learn:
   - Path A endpoint (v1.0) for Entra Agent ID create
   - Path B PowerShell cmdlet + Purview parameters
   - Both permission claims (app + delegated)
2. Hardened dry-run modes to be fully OFFLINE (no token acquisition, no network):
   - `--dry-run` / `-DryRun` now prints request shapes without connection
   - Reordered Path A least-privilege scopes (Create.All → CreateAsManager / ReadWrite.All)
3. Verified PATCH fallback path and open-type extension fields

## Files Produced

- `agent-intake/scripts/setup_entra_agent_id.py` (dry-run hardened)
- `agent-intake/scripts/setup_purview_retention_label.py` (dry-run hardened)
- `agent-intake/scripts/setup_purview_retention_label.ps1` (dry-run hardened, least-priv reordered)

## Outcome

All request shapes verified against Microsoft Learn. Dry-run paths fully OFFLINE. Ready for Saul's test coverage.

**Status:** ✓ Complete — decision logged
