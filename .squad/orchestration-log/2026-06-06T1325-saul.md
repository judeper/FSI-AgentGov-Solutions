# Orchestration Log: Saul (Tests)

**Date:** 2026-06-06T13:25:00-04:00
**Issue:** #123 agent-intake preflight
**Model:** claude-opus-4.8
**Mode:** background (test suite)

## Routing

**Role:** QA — lock request shapes + offline-safety regression tests
**Scope:** `agent-intake/tests/` (dry-run, create shapes, positive controls)

## Work

1. Added dry-run offline-safety tests (11 new assertions):
   - Monkeypatch all egress symbols (token providers + `requests` + `subprocess.run`) to raise
   - Assert `rc==0` on both setup paths with `-DryRun` / `--dry-run`
   - Positive-control tests prove sentinel is on live path (no vacuous passes)
2. Locked create request shapes:
   - Entra Agent ID: `POST /v1.0/servicePrincipals/microsoft.graph.agentIdentity` body keys + required fields
   - Purview retention label: `New-ComplianceTag` parameter structure
3. Graph API version: flagged URL constraint (asserts `beta` in sample; now GA at v1.0 — do not repoint without test update)

## Files Produced

- `agent-intake/tests/test_setup_entra_agent_id.py` (+5 tests)
- `agent-intake/tests/test_purview_retention_label.py` (+6 tests)

## Outcome

Test coverage: 40 → 51 passed. Ruff clean. Offline-safety is now a regression invariant.

**Status:** ✓ Complete — decision logged
