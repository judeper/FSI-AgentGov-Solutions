msal>=1.30.0
requests>=2.32.0
# azure-identity enables managed-identity, workload-identity, and certificate
# auth modes in scripts/shared/dataverse_client.py. Optional at runtime — only
# imported lazily when --auth-mode managed-identity / workload-identity /
# certificate is requested. Pinned >=1.15.0 for stable token caching APIs.
azure-identity>=1.15.0
