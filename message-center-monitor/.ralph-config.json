{
  "_comment": "Domain facts for message-center-monitor. Lock in column types, option-set mappings, entity-set names, and the C1 invariant location so future agents do not re-introduce the fsi_assessedby Lookup mistake or any of the other known foot-guns. Read this BEFORE editing any script in this solution. See also docs/dataverse-schema.md (auto-generated from scripts/create_mcm_dataverse_schema.py).",
  "version": "1.1",
  "lastUpdated": "2026-05-19",
  "domainFacts": [
    "fsi_assessedby is a String column (StringAttributeMetadata, MaxLength 200), NOT a Lookup. Never use _fsi_assessedby_value OData syntax - it returns 400 Bad Request. See scripts/create_mcm_dataverse_schema.py for the schema definition.",
    "Dataverse logical names are the SchemaName lowercased with NO underscores inserted between words. fsi_AssessedBy -> fsi_assessedby (NOT fsi_assessed_by); fsi_MessageCenterId -> fsi_messagecenterid; fsi_AssessmentStatus -> fsi_assessmentstatus.",
    "_<column>_value OData syntax is Lookup-only. For String, Choice (OptionSet), DateTime, Boolean, or any non-Lookup column, use the plain column name in $select and $filter.",
    "Severity option set values: 100000000=High, 100000001=Normal, 100000002=Critical. See scripts/create_mcm_dataverse_schema.py. Common mistake: assuming 0=Normal, 1=High - wrong.",
    "Severity option-set fsi_MCM_messageseverity uses integer values that are NOT naturally ordered by severity: 100000000=High, 100000001=Normal, 100000002=Critical. An OData filter like 'fsi_severity ge 100000001' does NOT mean 'at least High' — it means 'Normal OR Critical' (and EXCLUDES High). To filter by ranked severity, use explicit 'in (100000000,100000002)' lists or filter on the label after expansion.",
    "Category option set values: 100000000=Feature, 100000001=Admin, 100000002=Security. See scripts/create_mcm_dataverse_schema.py.",
    "Assessment status option set values: 100000000=NotAssessed, 100000001=Reviewed, 100000002=ImpactsAgents, 100000003=NoImpact. See scripts/create_mcm_dataverse_schema.py.",
    "Entity set name (table name in OData URL): fsi_messagecenterlogs (plural with 's'). Table SchemaName is fsi_MessageCenterLog (singular). See _Common.ps1 for the dvBaseUrl construction.",
    "The seven admin-owned columns on fsi_messagecenterlog are: fsi_assessmentstatus, fsi_assessment, fsi_assessedby, fsi_assesseddate, fsi_actionstaken, fsi_impactsagents, fsi_notifiedon. Invoke-McmDvUpsertMessage in scripts/governance/_Common.ps1 enforces that these columns are NEVER sent on the PATCH update branch (only on the conditional create-with-If-None-Match branch, where only fsi_assessmentstatus is seeded to NotAssessed). Any new human-curated column (assessment workflow, supervisor notes, etc.) must be added to this list, to the create-payload exclusion logic in _Common.ps1, AND to the set-intersection assertion in tests/Upsert.Tests.ps1.",
    "C1 enforcement location: Invoke-McmDvUpsertMessage in scripts/governance/_Common.ps1 deliberately rejects admin-owned columns in the $Record parameter. The invariant is verified by a STATIC REGEX TEST in tests/Sync.Tests.ps1 (lines 39-62) - NOT tests/Guards.Tests.ps1.",
    "Direct PATCH for admin-owned columns: when an admin column must be written from a governance script (e.g. fsi_notifiedon after a successful Teams notification), bypass Invoke-McmDvUpsertMessage and use Invoke-McmRest with a targeted single-field body. See Invoke-MessageCenterSync.ps1 for the canonical example.",
    "Idempotent upsert uses the alternate key fsi_MessageCenterIdKey on fsi_messagecenterid (the Graph ID, e.g. 'MC1296939'), NOT on fsi_messagecenterlogid (the system GUID primary key). The canonical upsert URL is: PATCH /api/data/v9.2/fsi_messagecenterlogs(fsi_messagecenterid='MC1296939') with header If-None-Match: * for create-only semantics. A 412 Precondition Failed response means the row already exists and the caller must fall through to the update branch (without admin-owned columns in the body).",
    "Alternate-key index activation is asynchronous on first deploy. lab/03_Deploy-Schema.ps1 polls EntityDefinitions(LogicalName='fsi_messagecenterlog')/Keys for EntityKeyIndexStatus = Active for up to 15 minutes (configurable). Attempts to PATCH via the alt-key URL before the index reaches Active return HTTP 404, even though the row exists. Production deployments must wait for the index to activate before enabling the sync flow.",
    "PowerShell governance scripts authenticate via Get-McmAccessToken in scripts/governance/_Common.ps1. Modes: ManagedIdentity (default and preferred), WorkloadIdentity, Interactive, DeviceCode, ClientSecret. ClientSecret is the POC fallback only - migrate to certificate or workload identity federation before production.",
    "Phase 1 (PowerShell webhook in Invoke-MessageCenterSync.ps1) and Phase 3 (Power Automate flow described in docs/flow-configuration.md) are MUTUALLY EXCLUSIVE notification paths. fsi_notifiedon provides per-row idempotency within ONE path only - it is NOT cross-path dedup. Test-McmPrerequisites.ps1 check #11 enforces the exclusion at deploy time.",
    "Schema documentation at docs/dataverse-schema.md is AUTO-GENERATED. Edit scripts/create_mcm_dataverse_schema.py and re-run with --output-docs to regenerate; do not hand-edit the markdown.",
    "site-docs/solutions/message-center-monitor/ is gitignored. Never edit there - it is regenerated by scripts/build-manifest.py from manifest.yaml and the per-solution docs/ folder.",
    "Teams Office 365 connector webhooks were retired by Microsoft in 2026-Q1. Use Teams Workflows incoming webhook (the 'Post to a channel when a webhook request is received' template). The shared Adaptive Card template at templates/teams-notification-card.json (Adaptive Card schema 1.5) is rendered by BOTH the Phase 1 PowerShell sync and the Phase 3 Power Automate flow - do not rename tokens or restructure the body without coordinating both paths.",
    "Adaptive card tokens supplied by the Phase 1 PowerShell sync: severity, title, category, services, startDateTime, actionRequiredByDateTime, id, environment, appId, publisherPrefix, recordId. recordId comes from the Dataverse upsert response body (fsi_messagecenterlogid), which requires 'Prefer: return=representation' on the request headers - already set by _Common.ps1.",
    "Governance scripts that make HTTP calls (anything under scripts/governance/ except _Common.ps1) MUST dot-source _Common.ps1 AND route ALL HTTP through Invoke-McmRest (not Invoke-RestMethod, not Invoke-WebRequest). Enforced by tests/Guards.Tests.ps1. Az CLI is forbidden in governance scripts; use the Az.KeyVault module instead. Both rules are enforced by tests/Guards.Tests.ps1.",
    "The canonical lab validation path is lab/06_Invoke-LabSmokeTest.ps1 (a 10-step orchestrator that exercises the v2.4.0 admin-field-preservation fix end-to-end with C1 regression assertions on all 7 admin-owned columns). Hand-authored flow JSON or pac solution import paths are NOT supported validation routes. Always run the numbered lab/ scripts in order (00 → 06, then 99 for teardown) against a non-prod environment. Every mutating lab script requires lab-config.json to contain the literal string 'I understand this lab must not target production' in nonProd.acknowledgement — the Assert-NonProdAcknowledgement guard is case-and-punctuation sensitive.",
    "The HTTP-to-Graph connector is shared_webcontents ('HTTP with Microsoft Entra ID'), NOT the deprecated shared_http Premium connector. When configuring the connection, AudienceUri must be the bare resource URI 'https://graph.microsoft.com' — NOT 'https://graph.microsoft.com/.default'. The /.default suffix is OAuth scope syntax used at token-acquisition time; placing it in an Audience claim causes AADSTS500011 ('resource principal not found in tenant') on every call.",
    "Manifest control mapping is '2.3' only — Control 2.10 (Patch Management) was removed in v2.4.0 as aspirational (this solution monitors changes; it does not patch anything). The historical Solution Catalog tables in README.md, AGENTS.md, CLAUDE.md, and .github/copilot-instructions.md may still list both — manifest.yaml is authoritative. Do not re-add 2.10 to the manifest without an actual patch-management control implementation.",
    "Service-principal connection references for cloud flows hit a documented security boundary: an SP-owned flow cannot bind a connection reference created under a different identity, and the maker portal hides the 'Use this connection' picker when the connection owner ≠ flow owner. Resolve by either (a) building the flow interactively as the human admin and exporting it, or (b) using HTTP-with-Microsoft-Entra-ID actions for everything outside Dataverse (which authenticate per-call instead of via a stored connection). The lab/ scripts take path (b)."
  ]
}
